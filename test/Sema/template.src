// V %srcc --sema --verify %s
// expected-no-diagnostics
program test;

proc add($T a, T b) = a + b;
proc add2(T a, $T b) = a + b;
proc add3($T a, $T b) = a + b;
proc add4($T a, $U b) = a + b;

add(1, 2);
add2(1, 2);
add3(1, 2);
add4(1, 2);
