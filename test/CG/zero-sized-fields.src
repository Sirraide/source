// R %srcc --ir --target x86_64-unknown-linux %s
program test;

struct zero {}
struct s {
    zero a;
    zero b;
    zero c;
}

proc a() extern -> zero;
proc b() extern -> zero;
proc c() extern -> zero;
proc d() extern -> s;

s e;
s(zero(), zero(), zero());

proc call {
    s(a(), b(), c());
    d();
}

// * proc __src_main external ccc {
// + entry:
// +     ret
// + }
// +
// + proc _S4callFvE.1 private ccc {
// + entry:
// +     call ccc void a()
// +     call ccc void b()
// +     call ccc void c()
// +     call ccc void d()
// +     ret
// + }
// +
// + proc a external ccc;
// +
// + proc b external ccc;
// +
// + proc c external ccc;
// +
// + proc d external ccc;
