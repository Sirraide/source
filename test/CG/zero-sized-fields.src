// R %srcc --ir --target x86_64-unknown-linux %s
program test;

struct zero {};
struct s {
    zero a;
    zero b;
    zero c;
};

proc a() extern -> zero;
proc b() extern -> zero;
proc c() extern -> zero;

s d;
s(zero(), zero(), zero());

proc call {
    s(a(), b(), c());
}

// * proc __src_assert_fail (ptr) external fastcc;
// +
// + proc __src_int_arith_error (ptr) external fastcc;
// +
// + proc __src_main external fastcc {
// + entry:
// +     ret
// + }
// +
// + proc _S4callFvE private fastcc {
// + entry:
// +     call fastcc void _S1aFT4zeroE()
// +     call fastcc void _S1bFT4zeroE()
// +     call fastcc void _S1cFT4zeroE()
// +     ret
// + }
// +
// + proc _S1aFT4zeroE external fastcc;
// +
// + proc _S1bFT4zeroE external fastcc;
// +
// + proc _S1cFT4zeroE external fastcc;
