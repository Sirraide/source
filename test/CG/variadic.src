// R      %srcc --ir --target x86_64-unknown-linux %s
// R[//L] %srcc --llvm --target x86_64-unknown-linux %s
program test;

proc f (int... x) {}
proc g (i32 y, i32... x) {}

proc q1 {
    f();
    f(1);
    f(1, 2);
    f(1, 2, 3);
}

proc q2 {
    g(42);
    g(42, 1);
    g(42, 1, 2);
    g(42, 1, 2, 3);
}