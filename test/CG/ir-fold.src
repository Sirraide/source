// R %srcc --ir -foverflow-checks=false --target x86_64-unknown-linux %s
program test;

// Test that our IR builder performs trivial constant folding to simplify
// the life of our compile-time evaluator a bit.
var a = 4 - 3;
var b = 4 + 3;
i64 c = 7;

// * @_S1a.1 = private 8, align 8
// + @_S1b.2 = private 8, align 8
// + @_S1c.3 = private 8, align 8
// +
// + proc __src_main norecurse external ccc {
// + entry:
// +     store @_S1a.1, i64 1, align 8
// +     store @_S1b.2, i64 7, align 8
// +     store @_S1c.3, i64 7, align 8
// +     ret
// + }
