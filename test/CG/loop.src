// R %srcc --ir -foverflow-checks=false --target x86_64-unknown-linux %s
program test;

proc a extern -> int;

proc d {
    loop { a(); }
}

proc e { loop a(); }
proc f { loop 4; }
proc g { loop; }
proc h = a() + loop;

var x = if 1 == 1 then 3 else loop;

// * @_S1x.1 = private 8, align 8
// +
// + proc __src_main norecurse external ccc {
// + entry:
// +     store @_S1x.1, i64 3, align 8
// +     ret
// + }
// +
// + proc _S1dFvE.1 private ccc {
// + entry:
// +     br bb1
// +
// + bb1:
// +     %0 = call ccc i64 a()
// +     br bb1
// + }
// +
// + proc a external ccc -> i64;
// +
// + proc _S1eFvE.2 private ccc {
// + entry:
// +     br bb1
// +
// + bb1:
// +     %0 = call ccc i64 a()
// +     br bb1
// + }
// +
// + proc _S1fFvE.3 private ccc {
// + entry:
// +     br bb1
// +
// + bb1:
// +     br bb1
// + }
// +
// + proc _S1gFvE.4 private ccc {
// + entry:
// +     br bb1
// +
// + bb1:
// +     br bb1
// + }
// +
// + proc _S1hFiE.5 private ccc -> i64 {
// + entry:
// +     %0 = call ccc i64 a()
// +     br bb1
// +
// + bb1:
// +     br bb1
// + }
