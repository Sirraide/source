// R %srcc --ir --short-filenames %s
program test;

proc sink (in int) {}
proc exp (in int a, in int b) {
    sink(a ** b);
    sink(a ** b);
    sink(a ** b);
}

proc exp_ass (copy int a, in int b) {
    a **= b;
}

