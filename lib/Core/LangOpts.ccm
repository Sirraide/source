export module srcc.langopts;

export namespace srcc {
struct LangOpts {
    // Enable overflow checking. When disabled, overflow is
    // undefined behaviour instead.
    bool overflow_checking : 1 = true;
};
}
