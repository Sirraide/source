program raylib_test;

import <raylib.h> as *;

InitWindow(800, 600, "Hello from Source".data);
SetTargetFPS(60);
while not WindowShouldClose() {
    BeginDrawing();
    ClearBackground(Color(255 as i8, 0, 0, 255 as i8));
    EndDrawing();
}