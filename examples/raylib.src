program raylib_test;

import <raylib.h> as *;

InitWindow(800, 600, "Hello from Source");
SetTargetFPS(60);
while not WindowShouldClose() {
    BeginDrawing();
    ClearBackground(RED);
    EndDrawing();
}
